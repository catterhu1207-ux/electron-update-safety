# Contributing

Use synthetic fixtures. Run `py -3 -m unittest discover -s tests -v` before submitting a change. Contributions must not include vendor binaries, extracted application code, credentials, or real user data.
